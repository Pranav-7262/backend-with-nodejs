// const add = require("./add.js");

// const mult = require("./add.js");

// const { add, sub, mult, div, PI } = require("./add.js"); // Destructuring to import specific functions

const math = require("./add.js"); // here  we are naming the all module as math

console.log(math.add(1288, 4));
console.log(math.sub(34, 5));
console.log(math.mult(2, 4));
console.log(math.div(50, 5));
console.log(math.PI);
